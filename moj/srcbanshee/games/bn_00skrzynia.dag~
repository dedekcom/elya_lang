game mg00_skrzynia :Banshee {
	init{
    
    Banshee::init("mg00_skrzynia");
  
      
    .imgs = (A, "bkg.jpg 1", "popup_bkg.pyz 2","ramka.pyz 3", "popexit.pyz 21");
      .butpopexit;
     
     .sfxs = (A, "bgrmorzeN 60");
     
     .newsndfree("sndtranskrypt", "kinnat_c1d1_2b");
     
    imgbkg {
		.addmethod("isbutin", func { (@x, @y)
			!anpopup_bkg.isin(x,y,0,0);
			} );
		.addmethod("butclick", func {
			if( !.advstdstate ) return;
			 .mend;
			} );
  		butgame.add(this);
  	}; 
         
    if (clsave.bis("e1_dzwonekblue_put") && clsave.bis("e1_dzwonekred_put")) 
      anpopup_bkg.play(2);
    else if (clsave.bis("e1_dzwonekblue_put") || clsave.bis("e1_dzwonekred_put")) 
      anpopup_bkg.play(1);
    else
       anpopup_bkg.play(0);
     
    .stdstart;  
  } // koniec init
  
    
    
  mend	{
    .stdexitadvadv("ad00_endintro", "gamead00_endintro0");  
	}
  
  HINT_CLICK {
    .showhint("anpopexit");  
  } 
}
 
